# 🚨 Процедура восстановления после атаки

Если вы обнаружили признаки SQL-injection атаки, следуйте этой процедуре.

## Шаг 1: Немедленное действие (2-5 минут)

### 1.1 Остановить приложение и изолировать БД

```bash
cd /opt/RUBLESTORE_GLOBAL

# Остановить все контейнеры
docker compose down

# Проверить, что всё остановлено
docker ps
```

### 1.2 Сохранить логи для анализа

```bash
# Сохранить все логи Docker
docker compose logs > /backup/docker-logs-attack-$(date +%Y%m%d-%H%M%S).log

# Найти логи PostgreSQL если используется отдельный контейнер
docker logs ruble-store-postgres > /backup/postgres-logs-attack-$(date +%Y%m%d-%H%M%S).log

# Сохранить системные логи
journalctl -u docker.service > /backup/system-logs-attack-$(date +%Y%m%d-%H%M%S).log
```

### 1.3 Сохранить том БД (если хотите анализировать поражённую БД)

```bash
# Если нужно посмотреть, что изменилось
docker run --rm -v ruble-store-postgres_data:/data \
  alpine tar czf /tmp/postgres-data-infected-$(date +%Y%m%d-%H%M%S).tar.gz -C / data

# Переместить резервную копию в безопасное место
mv /tmp/postgres-data-infected-*.tar.gz /backup/
```

## Шаг 2: Восстановление БД (10-15 минут)

### Вариант A: Если есть резервная копия (РЕКОМЕНДУЕТСЯ)

```bash
cd /opt/RUBLESTORE_GLOBAL

# 1. Удалить поражённый том
docker volume rm ruble-store-postgres_data

# 2. Восстановить из резервной копии
# (Предположим, резервная копия в /backup/postgres-backup-latest.sql.gz)
docker run --rm -v ruble-store-postgres_data:/var/lib/postgresql/data \
  -v /backup:/backup \
  postgres:16-alpine \
  bash -c "cd /var/lib/postgresql/data && gunzip -c /backup/postgres-backup-latest.sql.gz | psql -U postgres"

# 3. Перезапустить контейнеры
docker compose up -d
```

### Вариант B: Если резервной копии нет (ПОТЕРЯ ДАННЫХ)

```bash
cd /opt/RUBLE_STORE_GLOBAL

# 1. Удалить поражённый том (ВСЕ ДАННЫЕ ПОТЕРЯНЫ!)
docker volume rm ruble-store-postgres_data

# 2. Пересоздать контейнер (с новой чистой БД)
docker compose up -d --build

# 3. Переинициализировать данные
docker compose run --rm web sh -c \
  "NODE_OPTIONS='--no-experimental-fetch' pnpm --filter web db:migrate"

# 4. Если нужны начальные данные
curl "http://localhost:3001/api/seed?key=YOUR_SEED_SECRET"
```

## Шаг 3: Смена всех паролей и ключей (5-10 минут)

### 3.1 Создать новые надёжные пароли

```bash
# Сгенерируйте новые ключи
NEW_DB_PASSWORD=$(openssl rand -base64 32)
NEW_PAYLOAD_SECRET=$(openssl rand -base64 32)
NEW_ENCRYPTION_KEY=$(openssl rand -base64 32)

echo "DB_PASSWORD=$NEW_DB_PASSWORD"
echo "PAYLOAD_SECRET=$NEW_PAYLOAD_SECRET"
echo "NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=$NEW_ENCRYPTION_KEY"
```

### 3.2 Обновить .env файл

```bash
cd /opt/RUBLESTORE_GLOBAL

# Отредактируйте .env с новыми значениями
nano .env

# Или автоматически (если используете переменные выше)
cat > .env << EOF
DB_PASSWORD=$NEW_DB_PASSWORD
PAYLOAD_SECRET=$NEW_PAYLOAD_SECRET
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=$NEW_ENCRYPTION_KEY
PAYLOAD_PUBLIC_SERVER_URL=http://155.117.46.144:3001
NEXT_PUBLIC_API_URL=http://155.117.46.144:4000
CORS_ORIGIN=http://155.117.46.144:3001
EOF

# Проверить файл
cat .env
```

### 3.3 Перезапустить с новыми ключами

```bash
docker compose down
docker compose up -d --build
```

## Шаг 4: Анализ и выявление причины (30+ минут)

### 4.1 Определить момент первой атаки

```bash
# Просмотреть логи PostgreSQL в хронологическом порядке
docker compose logs postgres | grep -E "FATAL|ERROR|WARNING" | head -20

# Найти первый подозрительный запрос
docker compose logs postgres | grep -i "CREATE FUNCTION\|CREATE OR REPLACE\|system\|exec" | head -10
```

### 4.2 Найти источник атаки

```bash
# Проверить логи веб-сервера (если есть)
sudo tail -f /var/log/nginx/access.log | grep -i "union\|select\|insert\|drop\|create"

# Или логи docker
docker compose logs web | grep -i "error\|exception" | head -20

# Посмотреть какие порты были открыты
netstat -tlnp 2>/dev/null | grep LISTEN

# Проверить iptables правила
sudo iptables -L -n
```

### 4.3 Определить затронутые таблицы

```bash
# Подключиться к БД и проверить
docker exec -it ruble-store-postgres psql -U postgres -d ruble_store << EOF
\d postgres_*
SELECT * FROM pg_proc WHERE proname LIKE '%system%' OR proname LIKE '%exec%';
EOF
```

### 4.4 Временная шкала события

```bash
# Получить время создания/модификации файлов
ls -la /var/lib/docker/volumes/ruble-store-postgres_data/

# Проверить когда была последняя успешная резервная копия
ls -la /backup/ | sort -k 6,7
```

## Шаг 5: Проверка системы (30+ минут)

### 5.1 Проверить всё ли залеплено

```bash
# Убедиться, что no exposed ports
docker compose config | grep ports

# Должны видеть только 3001 и 4000 (web и api)
# PostgreSQL должен быть только в expose
```

### 5.2 Запустить проверку безопасности

```bash
bash scripts/security-check.sh
```

### 5.3 Проверить логи на ошибки

```bash
docker compose logs --tail 100 | grep -i "error\|fatal"
```

## Шаг 6: Укрепление системы

### 6.1 Firewall правила

```bash
# Убедитесь, что открыты ТОЛЬКО нужные порты
sudo ufw status numbered

# Если нужно закрыть порт PostgreSQL (если почему-то был открыт)
sudo ufw delete allow 5432
sudo ufw delete allow 5433

# Добавить правило для блокировки подозрительных IP (если известен источник)
ATTACK_IP="123.45.67.89"
sudo ufw insert 1 deny from $ATTACK_IP
```

### 6.2 Обновить все зависимости

```bash
cd /opt/RUBLESTORE_GLOBAL

# Обновить npm пакеты
pnpm update

# Пересобрать образы Docker
docker compose build --no-cache
docker compose up -d
```

### 6.3 Включить мониторинг

```bash
# Установить инструменты мониторинга (если ещё нет)
# Например, Prometheus + Grafana или DataDog

# Или простая ротация логов
sudo logrotate /etc/logrotate.conf -f
```

## Шаг 7: Настройка резервных копий (регулярно после восстановления)

### 7.1 Автоматическое резервное копирование БД

```bash
# Создать скрипт бекапа
cat > /opt/RUBLESTORE_GLOBAL/backup-db.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backup"
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
BACKUP_FILE="$BACKUP_DIR/postgres-backup-$TIMESTAMP.sql.gz"

# Проверить, что ContainerID существует
if ! docker ps | grep -q ruble-store-postgres; then
    echo "PostgreSQL контейнер не запущен"
    exit 1
fi

# Создать резервную копию
docker exec ruble-store-postgres pg_dump -U postgres -d ruble_store | \
    gzip > "$BACKUP_FILE"

# Оставить только последние 7 резервных копий
cd "$BACKUP_DIR"
ls -t postgres-backup-*.sql.gz | tail -n +8 | xargs rm -f

echo "Резервная копия создана: $BACKUP_FILE"
EOF

chmod +x /opt/RUBLESTORE_GLOBAL/backup-db.sh
```

### 7.2 Добавить в crontab (ежедневное резервное копирование)

```bash
# Отредактировать crontab
sudo crontab -e

# Добавить строку для ежедневного бекапа в 2:00 AM
0 2 * * * /opt/RUBLESTORE_GLOBAL/backup-db.sh >> /var/log/postgres-backup.log 2>&1
```

### 7.3 Тест восстановления из резервной копии

```bash
# Один раз в месяц проверить, что резервная копия работает:
# 1. На тестовом сервере
# 2. Или локально в Docker

docker run --rm -v /backup:/backup postgres:16-alpine \
    sh -c "gunzip -c /backup/postgres-backup-latest.sql.gz | head -20"
```

## Шаг 8: Коммуникация и документирование

### 8.1 Уведомить команду

- Отправить отчёт всем, кто может быть затронут (администраторы, разработчики)
- Описать что произошло, когда, и как исправлено

### 8.2 Создать POST-MORTEM отчёт

```markdown
## Post-Mortem отчёт

**Дата:** [дата атаки]
**Тип:** SQL Injection
**Источник:** [IP если известно]
**Длительность:** [с X до Y]
**Воздействие:** [какие данные потеряны/повреждены]

### Корневые причины:
1. [Причина 1]
2. [Причина 2]

### Что было сделано:
1. [Действие 1]
2. [Действие 2]

### Профилактические меры:
1. [Мера 1]
2. [Мера 2]

### Timeline:
- 12:00 - Обнаружена атака
- 12:05 - Приложение остановлено
- 12:30 - БД восстановлена из бекапа
- 13:00 - Система протестирована
```

### 8.3 Внести изменения в SECURITY.md

Обновите документацию с извлёченными уроками.

## Контрольный список после восстановления

- [ ] Логи сохранены для анализа
- [ ] БД восстановлена и работает
- [ ] Все пароли/ключи изменены
- [ ] Firewall правила проверены
- [ ] Безопасность проверена (`security-check.sh`)
- [ ] Резервное копирование настроено
- [ ] Мониторинг включен
- [ ] Команда уведомлена
- [ ] Post-mortem отчёт составлен
- [ ] Документация обновлена

---

## Контакты для помощи

Если нужна помощь:
1. Свяжитесь с командой разработки
2. Обратитесь к провайдеру хостинга (если используется облачный сервис)
3. Рассмотрите найм специалиста по информационной безопасности

