# На сервере .env не читается отсюда — заполняйте в каждом приложении отдельно:
#
# 1) apps/web/.env   — для Next.js + Payload (порт Postgres на хосте: 5433)
# 2) apps/api/.env   — для NestJS API
#
# Ниже шаблоны для копирования.
# ========== apps/web/.env ==========
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ruble_store
# PAYLOAD_SECRET=ваш-секрет-не-меньше-32-символов
# PAYLOAD_PUBLIC_SERVER_URL=http://155.117.46.144
# PAYLOAD_DISABLE_PUSH=true
# NEXT_PUBLIC_API_URL=http://155.117.46.144:4000
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=сгенерируйте-ключ-32-символа
#
# ========== apps/api/.env ==========
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ruble_store
# PORT=4000
# CORS_ORIGIN=http://155.117.46.144
