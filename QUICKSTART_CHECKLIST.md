# ⚡ Быстрый чек-лист безопасности

## Перед запуском на production (5-10 минут)

### ✅ Шаг 1: Обновить код
```bash
cd /opt/RUBLESTORE_GLOBAL
git pull origin main
```

### ✅ Шаг 2: Сгенерировать пароли
```bash
# Каждый раз НОВЫЕ пароли!
openssl rand -base64 32  # DB_PASSWORD
openssl rand -base64 32  # PAYLOAD_SECRET
openssl rand -base64 32  # NEXT_SERVER_ACTIONS_ENCRYPTION_KEY
```

### ✅ Шаг 3: Создать .env
```bash
cat > .env << 'EOF'
DB_PASSWORD=<paste-from-step-2>
PAYLOAD_PUBLIC_SERVER_URL=http://your-domain:3001
NEXT_PUBLIC_API_URL=http://your-domain:4000
CORS_ORIGIN=http://your-domain:3001
PAYLOAD_SECRET=<paste-from-step-2>
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=<paste-from-step-2>
EOF

# Проверить
cat .env
```

### ✅ Шаг 4: Проверить безопасность
```bash
bash scripts/security-check.sh
# Должно быть: ✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ
```

### ✅ Шаг 5: Запустить
```bash
# Удалить старую БД с паролем "postgres"
docker compose down -v

# Запустить с новыми паролями
docker compose up -d --build

# Проверить логи (5-10 минут на первый запуск)
docker compose logs -f web
```

### ✅ Шаг 6: Проверить работу
```bash
# Всё должно быть Up
docker compose ps

# Открыть в браузере
# Сайт: http://your-domain:3001
# Админка: http://your-domain:3001/admin
```

---

## 🚨 Если что-то пошло не так

### Ошибка: `password authentication failed`
```bash
# Порт 5432 открыт И БД была создана со старым паролем
# Решение: удалить том и пересоздать
docker compose down -v
docker compose up -d --build
```

### Ошибка: `Application error: a server-side exception has occurred`
```bash
# Посмотреть реальную ошибку
docker compose logs web --tail 50

# Вероятнее всего - пароль в .env не совпадает с реальным
# Проверить .env и пересоздать контейнер
cat .env
docker compose down -v
docker compose up -d --build
```

### Ошибка: `Connection refused on port 5432`
```bash
# PostgreSQL не запустился
# Проверить логи
docker compose logs postgres --tail 50

# Пересоздать контейнер
docker compose down -v
docker compose up -d
```

---

## 🔒 Критически важные моменты

❌ **НИКОГДА:**
- Не открывайте порт 5432 / 5433 в интернет
- Не используйте пароль "postgres"
- Не коммитьте .env в Git
- Не используйте sql.raw() с пользовательским вводом

✅ **ВСЕГДА:**
- Генерируйте новые пароли (32+ символа)
- Используйте параметризованные SQL запросы
- Запускайте security-check.sh перед развёртыванием
- Делайте резервные копии БД

---

## 📞 Документация

- **Полная информация:** [SECURITY.md](./SECURITY.md)
- **Развёртывание на сервер:** [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Восстановление при атаке:** [INCIDENT_RESPONSE.md](./INCIDENT_RESPONSE.md)
- **Настройка переменных:** [docs/ENV_SETUP.md](./docs/ENV_SETUP.md)

---

**Статус:** ✅ Готово к production  
**Дата проверки:** 8 февраля 2026

