# ============= КРИТИЧЕСКИ ВАЖНО =============
# НИКОГДА не используйте эти значения в production!
# Замените на сильные пароли (минимум 32 символа)
#
# Сгенерировать сильный пароль:
# openssl rand -base64 32
# =============================================

# PostgreSQL (один инстанс для Payload в Next.js и для NestJS API)
# ВАЖНО: Порт 5432 НЕ открыт во внешний интернет (используется expose вместо ports)
# Доступ только из контейнеров Docker
DB_PASSWORD=change-me-in-production-very-secure-password-min-32-chars

# Payload (Next.js app)
PAYLOAD_SECRET=your-secret-key-min-32-chars-change-in-production
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3001
# Отключить интерактивный push схемы (dev не зависает на вопросе create/rename). Использовать миграции.
# PAYLOAD_DISABLE_PUSH=true

# Ключ шифрования Server Actions (base64, 32 байта). Устраняет "Server Action was not found" в админке.
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=

# NestJS API
PORT=4000
CORS_ORIGIN=http://localhost:3001

# Frontend (опционально)
NEXT_PUBLIC_API_URL=http://localhost:4000
