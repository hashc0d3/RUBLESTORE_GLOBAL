# 🔒 Рекомендации по безопасности Ruble Store

## ⚠️ Критические проблемы, которые были исправлены

### 1. **SQL Injection в миграциях базы данных** ✅ ИСПРАВЛЕНО

**Проблема:** 
Миграции использовали конкатенацию строк для формирования SQL-запросов:
```typescript
// ❌ НЕБЕЗОПАСНО
const title = "'" + esc(p.title) + "'";
sql.raw(`INSERT INTO "products" (...) VALUES (${title}, ...)`)
```

Это позволяло злоумышленнику выполнить произвольный SQL-код.

**Решение:**
Используются параметризованные запросы:
```typescript
// ✅ БЕЗОПАСНО
sql`INSERT INTO "products" (...) VALUES (${p.title}, ...)`
```

PostgreSQL автоматически экранирует параметры и защищает от injection.

**Затронутые файлы:**
- `/apps/web/src/migrations/20260204_000001.ts` (категории)
- `/apps/web/src/migrations/20260204_000002.ts` (товары)

---

### 2. **Открытый порт PostgreSQL в интернет** ✅ ИСПРАВЛЕНО

**Проблема:**
`docker-compose.yml` открывал порт 5433:
```yaml
ports:
  - "5433:5432"  # ❌ БД доступна всему интернету!
```

Лог атаки показывает попытку выполнить вредоносный код через PostgreSQL.

**Решение:**
Заменено на `expose` — БД доступна только из контейнеров:
```yaml
expose:
  - "5432"  # ✅ Только для внутренней сети Docker
```

---

### 3. **Слабый пароль PostgreSQL** ✅ ИСПРАВЛЕНО

**Проблема:**
Пароль `postgres` жёстко закодирован везде:
```yaml
POSTGRES_PASSWORD: postgres  # ❌ Тривиальный пароль
```

Это стандартный пароль, легко подбирается брутфорсом.

**Решение:**
Используется переменная окружения с минимум 32-символьным паролем:
```yaml
POSTGRES_PASSWORD: ${DB_PASSWORD:-change-me-in-production-very-secure-password}
```

Установите в `.env`:
```env
DB_PASSWORD=your-very-secure-random-password-min-32-chars
```

Генерируйте сильный пароль:
```bash
openssl rand -base64 32
```

---

## 📋 Чек-лист безопасности

### Перед запуском в Production:

- [ ] **Установлены все переменные окружения:**
  - `DB_PASSWORD` — минимум 32 символа (случайная строка)
  - `PAYLOAD_SECRET` — минимум 32 символа
  - `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` — base64, 32 байта

- [ ] **PostgreSQL защищена:**
  - Порт 5432 НЕ открыт в интернет (`expose`, не `ports`)
  - Пароль сильный (генерируется, не жёсткий код)
  - Логирование включено (`log_statement=all`)

- [ ] **Приложение защищено:**
  - Все миграции используют параметризованные запросы
  - Нет `sql.raw()` с пользовательским вводом
  - CORS ограничен до доверенных источников

- [ ] **Инфраструктура:**
  - Firewall блокирует порты, кроме 80, 443, 3001, 4000
  - Регулярные бекапы БД
  - Мониторинг логов PostgreSQL на anomalies

---

## 🔍 Как отследить SQL-injection атаки?

Включены расширенные логи PostgreSQL. Проверяйте:

```bash
# Смотрите логи БД в реальном времени
docker compose logs postgres -f

# Ищите подозрительные запросы:
docker compose logs postgres | grep -i "CREATE FUNCTION\|system\|exec"
```

**Признаки атаки:**
- `CREATE OR REPLACE FUNCTION` с `system` или `exec`
- Странные base64-строки в STATEMENT
- Много ошибок `password authentication failed`

---

## 🛡️ Дополнительные меры безопасности

### 1. Обновляйте зависимости
```bash
pnpm outdated
pnpm update
```

### 2. Используйте WAF (Web Application Firewall)
- ModSecurity для nginx/Apache
- AWS WAF если на AWS
- Cloudflare WAF для DDoS + injection защиты

### 3. Регулярно проверяйте CVE
```bash
# Проверка уязвимостей
pnpm audit
```

### 4. Настройте ротацию логов
```bash
# Логи PostgreSQL могут быстро расти
docker exec ruble-store-postgres logrotate /etc/logrotate.conf
```

### 5. Ограничьте права доступа
```bash
# Суперпользователь только для миграций
# Обычный пользователь для приложения
POSTGRES_USER=app
POSTGRES_PASSWORD=app-password
# Создать в миграции: CREATE ROLE app NOSUPERUSER
```

---

## 📚 Ссылки

- [OWASP SQL Injection](https://owasp.org/www-community/attacks/SQL_Injection)
- [PostgreSQL Security](https://www.postgresql.org/docs/current/sql-syntax.html)
- [Node-Postgres Parameterized Queries](https://node-postgres.com/features/queries)
- [Docker Security Best Practices](https://docs.docker.com/engine/security/)

---

## 🚨 Если произошла атака

1. **Немедленно:**
   ```bash
   docker compose down  # Остановить контейнеры
   docker compose logs postgres > /tmp/attack-logs.txt  # Сохранить логи
   ```

2. **Восстановление:**
   ```bash
   # Используйте последний бекап
   docker volume rm ruble-store-postgres_data
   # Восстановите из резервной копии
   ```

3. **Анализ:**
   - Проверьте `/tmp/attack-logs.txt`
   - Найдите момент первой атаки в логах Docker хоста
   - Проверьте логи nginx/firewall на IP атакующего

4. **Предотвращение:**
   - Обновите все пароли
   - Переразверните с новыми переменными окружения
   - Добавьте IP в firewall блэклист

